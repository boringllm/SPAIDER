fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
httpx>=0.27
anthropic>=0.40
openai>=1.40
python-multipart>=0.0.9
# Reference-document attachments (operator uploads md/txt/pdf/docx to inform the engagement).
# Optional: the app degrades gracefully if these are missing — only PDF/Word extraction is affected.
pypdf>=4.0
python-docx>=1.1
